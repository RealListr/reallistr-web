'use client';

import React from 'react';

/**
 * Minimal placeholder for the property detail client component.
 * Previously rendered AgentCluster; now intentionally empty.
 */
export default function DetailClient(_props: any) {
  return null; // keep layout stable without Agents feature
}
